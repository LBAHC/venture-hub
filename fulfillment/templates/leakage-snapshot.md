# Template — Leakage Snapshot (Day 1–2 deliverable, one page)

Purpose: the document that justifies the $500 on its own and pre-sells the retainer. Every number computed from THEIR book export — no benchmarks presented as their data. Benchmarks always labeled as benchmarks.

Format: one-page PDF (render from this markdown). Dollars first. No charts unless one chart earns its place (renewals-by-month bar is usually the one).

---

## {AGENCY NAME} — Leakage Snapshot
*Prepared {date} · from your book export of {export_date} · AgencyRetain*

### Your next 120 days
| | |
|---|---|
| Policies renewing in 45–90 days (sprint window) | **{n_sprint}** |
| Premium attached to those renewals | **${premium_sprint}** |
| Policies renewing in 91–120 days (next wave) | {n_next} |
| Premium attached | ${premium_next} |

### Where the leakage risk sits
- **{n_single_line} of {n_sprint} sprint-window policies are single-line households.** Single-line clients defect at roughly 2–3× the rate of multi-line clients (industry benchmark, not your data — your book doesn't record defections it never saw coming).
- **{n_high_premium} policies over ${premium_threshold}** renew in the window — these get producer-call priority, not just email.
- {other_finding — e.g., carrier concentration, a renewal cluster month, aged quotes found in export}

### The sprint math
If the campaign holds just **{conservative_pct}%** of the at-risk premium that would otherwise quietly shop — that's **${conservative_dollars}** in retained premium against a $500 sprint. We'll report actuals, not this estimate, on Day 14.

### What happens next
Campaign draft reaches you {date+1} for approval. First send {date+2} after your sign-off.

---
*Method note: figures computed from your export; benchmark rates cited are industry figures and labeled as such. We never present estimates as results.*
